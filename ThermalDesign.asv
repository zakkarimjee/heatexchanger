clear 

TcIn = 20;
ThIn = 60;
mc = 2;
mh = 4;

Nui = 0.023*Re
hi = 
U = (1/hi + di*log(d0/di)/2*Ktube + di/(d0*ho);
A = pi*di*L*NoT;

syms Q TcOut ThOut 
% syms x y a

% [solx,soly] = solve(x^2*y^2 == 0, x-y/2 == a);
[solx,soly,solz] = vpasolve(mc*(TcOut-TcIn)==Q, mh*(ThIn - ThOut)==Q, Q==U*A*((ThIn-TcOut)-(ThOut-TcIn)/log((ThIn-TcOut)/(ThOut-TcIn))))